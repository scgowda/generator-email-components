language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-lavender-email' ]; then cd .. && eval "mv $currentfolder generator-lavender-email" && cd generator-lavender-email; fi
